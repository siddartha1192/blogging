node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.db
*.sqlite
*.sqlite3
instance/
.pytest_cache/
.coverage
htmlcov/

# Environment variables
.env
.flaskenv

# Virtual environment
venv/
env/
ENV/

# Docker volumes / certs
postgres_data/
certbot/

# Generated SSL nginx config (produced by init-ssl.sh from nginx/nginx.conf template)
nginx/nginx-ssl.conf

# Database backups — keep locally, never commit
backups/

# Nginx log files — rotated/cleaned automatically, never commit
logs/nginx/*.log
logs/nginx/*.log.*

# Runtime uploads are managed by the Docker volume — do NOT commit them.
# To deploy images via Git, put them in static/seed/ instead.
static/uploads/
!static/uploads/.gitkeep

# static/seed/ IS tracked — this is where you commit images for Git-based deployment.
!static/seed/